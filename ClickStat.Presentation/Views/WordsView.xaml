<UserControl x:Class="ClickStat.Presentation.Views.WordsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="Transparent">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>

        <Style x:Key="SectionTitle" TargetType="TextBlock">
            <Setter Property="Foreground"  Value="White"/>
            <Setter Property="FontSize"    Value="14"/>
            <Setter Property="FontWeight"  Value="SemiBold"/>
            <Setter Property="Margin"      Value="0,0,0,10"/>
        </Style>

        <Style x:Key="WordChip" TargetType="Border">
            <Setter Property="Background"      Value="#1A1A2E"/>
            <Setter Property="CornerRadius"    Value="6"/>
            <Setter Property="BorderBrush"     Value="#2E2E5E"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding"         Value="10,5"/>
            <Setter Property="Margin"          Value="0,0,6,6"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <TextBlock Text="Загрузка…" Foreground="#A0A0C0" FontSize="18"
                   HorizontalAlignment="Center" VerticalAlignment="Center"
                   Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}"/>

        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <ScrollViewer.Style>
                <Style TargetType="ScrollViewer">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsLoading}" Value="False">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </ScrollViewer.Style>

            <StackPanel>

                <!-- Stats bar -->
                <Border Background="#0E0E1C" CornerRadius="12" Padding="16,12"
                        Margin="0,0,0,20" BorderBrush="#1A1A4A" BorderThickness="1">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <StackPanel Margin="0,0,32,0" HorizontalAlignment="Center">
                            <TextBlock Text="Всего слов напечатано" Foreground="#606080" FontSize="11"/>
                            <TextBlock Text="{Binding TotalTyped}" Foreground="White" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center"/>
                        </StackPanel>
                        <StackPanel Margin="0,0,32,0" HorizontalAlignment="Center">
                            <TextBlock Text="Уникальных слов" Foreground="#606080" FontSize="11"/>
                            <TextBlock Text="{Binding UniqueCount}" Foreground="White" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center"/>
                        </StackPanel>
                        <StackPanel HorizontalAlignment="Center">
                            <TextBlock Text="Слов после фильтра" Foreground="#606080" FontSize="11"/>
                            <TextBlock Text="{Binding KnownPercent, StringFormat='{}{0}%'}"
                                       Foreground="#AA70FF" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="16"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Left column: words -->
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="📚 Слова" Style="{StaticResource SectionTitle}"/>
                        <Border Background="#0E0E1C" CornerRadius="10" Padding="12">
                            <ItemsControl ItemsSource="{Binding KnownWords}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate><WrapPanel/></ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Style="{StaticResource WordChip}">
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="{Binding Word}" Foreground="#C0C0E0" FontSize="12"/>
                                                <TextBlock Text=" " FontSize="12"/>
                                                <TextBlock Text="{Binding Count}" Foreground="#AA70FF" FontSize="12" FontWeight="Bold"/>
                                            </StackPanel>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </Border>

                        <TextBlock Text="🔤 Прочие токены" Style="{StaticResource SectionTitle}" Margin="0,20,0,10"/>
                        <Border Background="#0E0E1C" CornerRadius="10" Padding="12">
                            <ItemsControl ItemsSource="{Binding UniqueWords}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate><WrapPanel/></ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Style="{StaticResource WordChip}">
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="{Binding Word}" Foreground="#909090" FontSize="12" FontFamily="Consolas"/>
                                                <TextBlock Text=" " FontSize="12"/>
                                                <TextBlock Text="{Binding Count}" Foreground="#6060A0" FontSize="12" FontWeight="Bold"/>
                                            </StackPanel>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </Border>
                    </StackPanel>

                    <!-- Right column: bigrams + phrases -->
                    <StackPanel Grid.Column="2">
                        <TextBlock Text="⌨️ Частые пары клавиш (биграммы)" Style="{StaticResource SectionTitle}"/>
                        <Border Background="#0E0E1C" CornerRadius="10" Padding="12">
                            <ItemsControl ItemsSource="{Binding TopBigrams}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Margin="0,0,0,6">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Text="{Binding Pair}" Foreground="#909090"
                                                       FontFamily="Consolas" FontSize="12" VerticalAlignment="Center"/>
                                            <TextBlock Grid.Column="1" Text="{Binding Count}"
                                                       Foreground="#AA70FF" FontSize="13" FontWeight="Bold"/>
                                        </Grid>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </Border>

                        <TextBlock Text="💬 Частые фразы" Style="{StaticResource SectionTitle}" Margin="0,20,0,10"/>
                        <Border Background="#0E0E1C" CornerRadius="10" Padding="12">
                            <ItemsControl ItemsSource="{Binding TopPhrases}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Margin="0,0,0,6">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Text="{Binding Phrase}" Foreground="#C0C0E0"
                                                       FontSize="12" VerticalAlignment="Center"/>
                                            <TextBlock Grid.Column="1" Text="{Binding Count}"
                                                       Foreground="#AA70FF" FontSize="13" FontWeight="Bold"/>
                                        </Grid>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </Border>
                    </StackPanel>
                </Grid>

            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
