<UserControl x:Class="ClickStat.Presentation.Views.WordsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="Transparent">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>

        <Style x:Key="SectionTitle" TargetType="TextBlock">
            <Setter Property="Foreground"  Value="White"/>
            <Setter Property="FontSize"    Value="14"/>
            <Setter Property="FontWeight"  Value="SemiBold"/>
            <Setter Property="Margin"      Value="0,0,0,10"/>
        </Style>

        <Style x:Key="WordChip" TargetType="Border">
            <Setter Property="Background"      Value="#1A1A2E"/>
            <Setter Property="CornerRadius"    Value="6"/>
            <Setter Property="BorderBrush"     Value="#2E2E5E"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding"         Value="10,5"/>
            <Setter Property="Margin"          Value="0,0,6,6"/>
        </Style>

        <Style x:Key="LanguageButton" TargetType="Button">
            <Setter Property="Background" Value="#1A1A2E"/>
            <Setter Property="Foreground" Value="#CFCFFF"/>
            <Setter Property="BorderBrush" Value="#2E2E5E"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="14,7"/>
            <Setter Property="Margin" Value="0,0,8,0"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <TextBlock Text="Загрузка…" Foreground="#A0A0C0" FontSize="18"
                   HorizontalAlignment="Center" VerticalAlignment="Center"
                   Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}"/>

        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <ScrollViewer.Style>
                <Style TargetType="ScrollViewer">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsLoading}" Value="False">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </ScrollViewer.Style>

            <StackPanel>

                <!-- Stats bar + language switch -->
                <Border Background="#0E0E1C" CornerRadius="12" Padding="16,12"
                        Margin="0,0,0,20" BorderBrush="#1A1A4A" BorderThickness="1">
                    <Grid>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center">
                            <Button TextBlock.TextAlignment="Center"
                                    Content="{Binding RussianButtonText}"
                                    Command="{Binding ShowRussianCommand}"
                                    Style="{StaticResource LanguageButton}"/>
                            <Button Content="{Binding EnglishButtonText}"
                                    Command="{Binding ShowEnglishCommand}"
                                    Style="{StaticResource LanguageButton}"/>
                        </StackPanel>

                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <StackPanel Margin="0,0,32,0" HorizontalAlignment="Center">
                                <TextBlock Text="Язык" Foreground="#606080" FontSize="11"/>
                                <TextBlock Text="{Binding LanguageTitle}" Foreground="#AA70FF" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center"/>
                            </StackPanel>
                            <StackPanel Margin="0,0,32,0" HorizontalAlignment="Center">
                                <TextBlock Text="Всего слов" Foreground="#606080" FontSize="11"/>
                                <TextBlock Text="{Binding TotalTyped}" Foreground="White" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center"/>
                            </StackPanel>
                            <StackPanel Margin="0,0,32,0" HorizontalAlignment="Center">
                                <TextBlock Text="Уникальных слов" Foreground="#606080" FontSize="11"/>
                                <TextBlock Text="{Binding UniqueCount}" Foreground="White" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center"/>
                            </StackPanel>
                            <StackPanel HorizontalAlignment="Center">
                                <TextBlock Text="Фраз" Foreground="#606080" FontSize="11"/>
                                <TextBlock Text="{Binding PhraseCount}" Foreground="White" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center"/>
                            </StackPanel>
                        </StackPanel>
                    </Grid>
                </Border>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="16"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Left column: words -->
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="📚 Слова выбранного языка" Style="{StaticResource SectionTitle}"/>
                        <Border Background="#0E0E1C" CornerRadius="10" Padding="12">
                            <ItemsControl ItemsSource="{Binding TopWords}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate><WrapPanel/></ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Style="{StaticResource WordChip}">
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="{Binding Word}" Foreground="#C0C0E0" FontSize="12"/>
                                                <TextBlock Text=" " FontSize="12"/>
                                                <TextBlock Text="{Binding Count}" Foreground="#AA70FF" FontSize="12" FontWeight="Bold"/>
                                            </StackPanel>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </Border>
                    </StackPanel>

                    <!-- Right column: phrases -->
                    <StackPanel Grid.Column="2">
                        <TextBlock Text="💬 Частые фразы выбранного языка" Style="{StaticResource SectionTitle}"/>
                        <Border Background="#0E0E1C" CornerRadius="10" Padding="12">
                            <ItemsControl ItemsSource="{Binding TopPhrases}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Margin="0,0,0,6">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Text="{Binding Phrase}" Foreground="#C0C0E0"
                                                       FontSize="12" VerticalAlignment="Center"/>
                                            <TextBlock Grid.Column="1" Text="{Binding Count}"
                                                       Foreground="#AA70FF" FontSize="13" FontWeight="Bold"/>
                                        </Grid>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </Border>
                    </StackPanel>
                </Grid>

            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
